# KitchenSight Agent MVP 需求设计

## 1. 项目定位

KitchenSight Agent 是一套面向奶茶店一线店员、尤其是新员工的第一视角工作流智能体。它以普通 AI 眼镜为目标终端，但 MVP 使用手机实时摄像头模拟眼镜的第一视角，以网页承载订单、Agent、语音交互和管理面板。

项目的目标不是替代店员，也不是持续监控店员；它在订单制作过程中提供低打扰的即时辅助，以提高订单处理效率、规范制作流程并降低新员工上手门槛。

## 2. 初赛 MVP 目标

验证以下假设：在模拟奶茶制作场景中，第一视角 Agent 能帮助单个新人店员更快、更规范地完成定制订单，并留下可复核的异常证据与培训数据。

MVP 成功标准：

- 完成一条从接单到出杯核验的可录制、可重复演示链路。
- 能处理 3 款虚构饮品及糖度、冰量、加料三类定制项。
- 能自动识别至少三类事件：正确取料、取错原料、漏加指定加料。
- 出现异常时，在店员纠正后将订单状态恢复为可完成。
- 管理面板可展示订单处理时长、异常类别、人工确认次数与最终状态。

## 3. 用户与场景

### 主用户

- 奶茶店门店店员；以新员工为主要受益者，熟练员工同样可使用。

### 次用户

- 店长或培训者，只读查看订单效率、异常记录和新人常见错误。

### 演示场景

- 单个店员在单一制作工位处理连续订单。
- 以手机后摄像头置于胸前或固定支架上，模拟 AI 眼镜的第一视角。
- 电脑浏览器运行模拟 KDS、Agent 控制台与店长面板。
- 演示物料使用虚构品牌、虚构配方和带清晰视觉标签/二维码的杯子、原料容器与加料道具。

## 4. 核心工作流

1. 模拟 KDS 下发订单，包含饮品、糖度、冰量、加料和优先级。
2. Agent 加载该订单的配方步骤与定制规则，使用语音播报开始制作。
3. 手机持续上传第一视角画面；视觉模块识别贴有演示标签的原料和杯子。
4. Agent 根据状态机判断当前动作是否符合订单所需步骤。
5. 对正确关键步骤作简短确认；对疑似取错料、漏加料或顺序异常主动语音提示。
6. 店员可用语音确认、重试或请求帮助；在低置信度情况由店员补充确认。
7. 出杯核验通过后，Agent 标记订单完成并将结构化事件写入管理数据。
8. 店长面板展示处理时长、异常、确认和订单状态。

## 5. 交互设计

### 语音优先

- Agent 仅在开始制作、特殊定制项、关键风险、完成/异常四类节点主动播报。
- 店员可说“开始订单”“确认”“重试”“跳过”“重复提示”“请求帮助”。
- 手机不承担主交互，只显示配方详情、连接状态与异常人工复核。

### 眼镜兼容原则

- 音频型 AI 眼镜：以摄像头、麦克风、开放式扬声器实现完整流程。
- 带显示的眼镜：仅增加一行短提示，例如订单号、特殊要求、步骤编号或“需复核”。
- 不依赖房间级空间建模、手势控制或持续眼动数据。

## 6. 功能边界

### 本期包含

- 虚构菜单：3 款基础饮品。
- 定制项：糖度、冰量、加料。
- 模拟 KDS 订单队列和订单详情。
- 手机实时摄像头采集与网页端实时预览。
- 基于视觉标签/二维码的物料识别与规则状态机。
- 浏览器端语音合成播报；语音指令可作为增强功能。
- 三类异常：取错原料、漏加料、制作顺序异常。
- 异常事件片段、店员确认保存和一键删除。
- 只读店长面板。

### 明确不包含

- 真实 POS/KDS、库存、支付或外卖平台对接。
- 多店员、跨工位协同与排班调度。
- 覆盖所有奶茶菜单、真实品牌配方或真实门店数据。
- 食品熟度、中心温度、过敏原接触等不能仅凭视觉可靠判定的安全结论。
- 真实智能眼镜原生 App 与任何厂商 SDK 强绑定。
- 对员工的连续录像、绩效监控或自动处罚。

## 7. 技术设计

### 系统组件

| 组件 | 职责 |
| --- | --- |
| 手机采集页 | 获取摄像头画面、传送视频帧、接收语音和短提示；未来替换为眼镜端。 |
| Web Agent 服务 | 维护订单状态、调用视觉识别、执行配方规则和生成提示。 |
| 模拟 KDS | 生成订单、展示队列并向 Agent 推送当前订单。 |
| 视觉识别模块 | 识别演示标签/二维码与杯子状态；为每项识别提供置信度。 |
| 语音模块 | 将 Agent 事件转换为中文语音播报；接收必要的店员指令。 |
| 事件与回放模块 | 保存结构化事件，并管理异常前后视频缓冲区。 |
| 店长面板 | 汇总订单耗时、异常、确认次数与结果。 |

### Agent 决策方式

- 使用“订单配方状态机 + 视觉事件 + 规则引擎”完成可解释、可重复的 MVP 决策。
- 大模型仅用于自然语言解释、追问和后续扩展；不作为食品安全或关键订单核验的唯一依据。
- 视觉置信度低时，Agent 不直接判错，而是请求店员确认。

## 8. 异常事件片段与隐私

1. 视频画面只在设备内存中保留滚动缓冲区，默认不连续落盘。
2. 缓冲区包含异常发生前 10 秒画面。
3. 识别到疑似异常后，继续保留后 10 秒画面，形成约 20 秒事件片段。
4. Agent 以语音提示店员确认是否保存；未确认的片段自动删除。
5. 管理面板默认仅显示订单号、事件类别、发生时间和处理结果；原始片段需显式授权才可查看。
6. 所有演示数据为虚构数据；不采集顾客人脸、支付信息或真实门店经营数据。

## 9. Demo 脚本

目标订单：A102，虚构饮品“云朵乌龙”，少糖、去冰、加珍珠。

1. KDS 出现 A102；Agent 语音简述定制项并开始计时。
2. 店员拿杯、取茶底；Agent 确认步骤。
3. 店员故意拿起不匹配的加料容器；Agent 识别后提示“当前订单需珍珠，请复核加料”。
4. 店员放回错误容器并拿取珍珠；Agent 确认纠正。
5. 店员完成封杯/贴标；Agent 核验定制项并宣布完成。
6. 店长面板显示本单耗时、一次“取错原料”异常、一次人工确认和完成状态。
7. 演示异常事件片段：取料前、取错、提示与纠正的完整上下文。

## 10. 评估指标

- 单笔订单完成时长。
- 标准步骤完成率。
- 漏配/错配发现率与纠正率。
- 每单人工确认次数。
- 异常从发生到提醒的时间。
- 异常事件片段保存率与删除率。

## 11. 后续扩展路线图

- 多店员、跨工位订单分配与高峰期协同。
- 与真实 POS/KDS、库存和培训系统对接。
- 从奶茶店扩展到咖啡、快餐、正餐后厨和中央厨房。
- 从视觉标签/二维码演示识别升级为多模态视觉模型与配方视觉知识库。
- 接入音频型及带显示的 AI 眼镜，加入镜片短提示。
- 加入仪器数据：电子秤、温度计、条码枪、封口机或 IoT 信号，用于可靠食品安全核验。
- 门店级匿名化学习分析与课程化新人培训。

## 12. 风险与缓解

| 风险 | 缓解措施 |
| --- | --- |
| 实时视觉识别不稳定 | 用高对比标签/二维码、固定机位、明确光照；低置信度转人工确认。 |
| 无智能眼镜硬件 | 手机第一视角替代；采集端接口独立，未来可替换。 |
| 后厨噪声影响语音 | 语音提示短句化；保留手机短文字和确认按钮作为兜底。 |
| 被误解为监控 | 默认不录像，异常片段须确认保存，禁止用于自动处罚。 |
| 范围失控 | 严格限制单工位、3 款饮品、3 类定制项与三类异常。 |

