# 杯序 CupFlow｜项目交接记录

更新日期：2026-08-14

## 1. 项目定位

- 赛事：GOAI 的 Boundless Agents 赛道。
- 项目名称：**杯序 CupFlow**。
- 场景：奶茶店门店新人店员的第一视角操作辅助智能体。
- MVP 形态：手机摄像头模拟 AI 眼镜；网页实时读取画面，理解订单与操作标记，逐步提示、发现异常、自动纠错并记录异常事件。
- 核心价值：提升订单处理效率、规范制作流程、降低新人上手门槛。
- 不包含 SLAM 或具身智能方向要求。

## 2. 已完成的网页 MVP

- 单屏录制友好的界面与品牌标题：`杯序 CupFlow｜奶茶店第一视角操作智能体`。
- 示例订单：A102「云朵乌龙奶茶」，少糖、去冰、加珍珠。
- 流程引导：取杯 → 加珍珠 → 加茶汤 → 定量 → 封口 → 杯贴核验。
- 摄像头实时识别为第一版 Demo 使用颜色/标记方案：
  - 白色杯身纸：识别取杯；
  - 粉色标签：珍珠；
  - 橙棕色标签：茶汤；
  - 浅绿色标签：椰果（错误小料）；
  - 红色标签：封口；
  - 浅黄绿色杯贴角标：正确订单杯贴；
  - 蓝色杯贴角标：错误订单杯贴。
- 异常演示逻辑：错加椰果、重复加珍珠、超量（演示控制）、错杯贴。
- 自动纠错：异常出现后，店员移除错误物料或展示正确标记，Agent 自动继续下一步，无需电脑端再次确认。
- 异常记录与复盘：展示异常类型、自动纠正状态、事件片段提示（异常前后 10 秒的设计口径）和管理视图。

## 3. 已准备的实体演示物料

- 杯子、量杯、茶汤、牛奶、珍珠、椰果、糖浆等。
- 杯贴两张：
  - 正确：少糖｜去冰｜加珍珠，浅黄绿色角标；
  - 错误：正常糖｜去冰｜加椰果，蓝色角标。
- 白纸贴在杯身上用于第一步“取杯”识别。

## 4. 网页与部署状态

- 本地源码目录：`/Users/a1/Desktop/goai/kitchensight-agent`
- 本地开发网址：`http://localhost:3000/`
- 已发布公开网址：
  `https://cupflow-milktea-agent-20260814.t7kc8mnyb6.chatgpt.site`
- 该公开网址在中国大陆网络中可能无法稳定访问，因此不建议作为他人演示的唯一方案。
- 已通过 `npm run build` 验证网页构建。

## 5. Windows 免安装版状态

- 已加入 Electron 便携桌面版构建配置与启动代码。
- 已验证在 macOS 上，Electron 内置 Node 环境能够启动 CupFlow 本地生产服务，并返回正常网页。
- 已生成最终 Windows 分享包：
  `/Users/a1/Desktop/CupFlow-Windows-Portable-Share.zip`
- 解压后预期启动文件：`START_CUPFLOW.bat`；主程序：`CupFlow-Windows-Portable.exe`。
- Windows 端当前问题：用户反馈双击 `.bat` 和 `.exe` 都会报错，但尚未提供错误截图或文字。**下一步需先获取完整报错截图，再定位修复并重新打包。**
- 不要再使用旧包 `CupFlow-Windows.zip`；它依赖外部 Node.js。

## 6. 代码与版本状态

- Git 已初始化，本地工作区在交接时为干净状态。
- 关键提交：
  - `950125f`：Deploy CupFlow MVP
  - `a416b57`：Add Windows portable desktop build
- 关键文件：
  - `app/page.tsx`：网页 Agent 的主要交互和识别逻辑；
  - `app/globals.css`：界面样式；
  - `electron/main.cjs`：Electron 启动本地服务并打开桌面窗口；
  - `package.json`：`npm run desktop:build` 生成 Windows 64 位便携程序；
  - `electron/START_CUPFLOW.bat`：一键启动文件。

## 7. 下一步建议（赛事提交）

1. 收集 Windows 端错误截图，修复并重新生成免安装包；或直接在 Mac 本地网页完成比赛 Demo 录制。
2. 使用手机摄像头、已准备物料录制完整 Demo：正确流程、错加椰果自动纠错、重复珍珠警报与纠错、超量警报、错杯贴纠错、异常记录。
3. 后期加入旁白与字幕；网页保留正常界面，不展示录制脚本文字。
4. 根据 GOAI 初赛官方要求整理提交文本、项目说明、Demo 视频链接及所需材料。
5. 如需要继续开发，优先把颜色标记 Demo 的识别逻辑表述为“低成本可复现的 MVP 视觉原型”，并明确未来可升级为真实视觉识别模型。
